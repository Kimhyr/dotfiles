#!/bin/env sh

# .config
CONFIGS=(fish foot helix sway)
mkdir -p .config 
for CONFIG in "${CONFIGS[@]}"; do
	cp -r ~/.config/$CONFIG .config
done

pacman -Qenq > packages.txt
pacman -Qemq > aurpackages.txt
